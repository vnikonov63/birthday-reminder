<script defer src="/js/admin.js"></script>

<h1 class="mb-2">Список студентов</h1>
<hr class="mb-4">

<form method="POST" action="/admin/sort" class="mb-3" id="sort" name="sort">


  <div class="input-bar-item" style="width: 30rem;">
    <form class="form-inline">
      <div class="input-group">
        <select class="form-control mb-2" id="select">
          <option id="upcoming" value="upcoming">По ближайшему дню рождения</option>
          <option id="lastName" value="name">По фамилии</option>
          <option id="default" value="default">По умолчанию</option>
          <option id="firstName" value="firstName">По имени</option>
        </select>
        <span class="input-group-btn">
          <input type="submit" class="btn btn-info ml-3" value="Отсортировать">
        </span>
      </div>
    </form>
  </div>



</form>

</form>
<div id="wrapper">
  {{#if students}}
  <ul id="students" class="list-group mb-5">
    {{#each students}}
    <li class="list-group-item li d-flex align-items-center justify-content-between" id="_{{this.id}}">
      <span id="{{this.id}}" class="title align-middle mr-auto">{{firstName}} {{lastName}} | {{typeBootCamp}} |
        <strong>Дата рождения: </strong>{{prettyDate}} | Группа: {{groupName}}
      </span>
      <a href="/admin/edit/{{this.id}}"><button type="button" class="btn btn-primary mr-3">Изменить</button></a>
      <button type="button" class="btn btn-danger">Удалить</button>
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>Список студентов пуст 😔</p>
  {{/if}}
</div>
